{
  "name": "modern-saas-template",
  "version": "1.0.0",
  "description": "Unified development environment for SaaS template",
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix frontend\" \"dotnet watch run --project ModuleDrivenFramwork\"",
    "db:up": "docker-compose up -d",
    "db:down": "docker-compose down"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
